# Third-Party Notices / 第三方声明

This document describes the current third-party software inventory that is
observable from `package-lock.json`, the checked-in package manifests, and the
runtime behavior documented in this repository.

本文件描述当前仓库中可由 `package-lock.json`、已检入的包清单以及仓库文档中可见
运行时行为证明的第三方软件清单。

It does not treat local research archives or excluded historical material as
shipped product dependencies. In particular, `reference-extensions/` is not part
of the public product package allowlist and is not listed here as a current
product dependency source.

本文件不把本地研究归档或已排除的历史材料视为产品依赖。尤其是
`reference-extensions/` 不在公开产品包 allowlist 中，因此这里不会把它作为当前
产品依赖来源来陈述。

## Inventory Basis / 清单依据

Current npm dependency licenses observed from `package-lock.json`:

当前从 `package-lock.json` 观察到的 npm 依赖许可证分布：

| License | Observed packages |
|---|---:|
| `MIT` | 201 |
| `Apache-2.0` | 94 |
| `BSD-3-Clause` | 25 |
| `MPL-2.0` | 12 |
| `ISC` | 10 |
| `BlueOak-1.0.0` | 5 |
| `0BSD` | 2 |

This means the current tree is not limited to MIT, Apache-2.0, and BSD-family
licenses. The lockfile also includes MPL-2.0 and BlueOak-1.0.0 packages, which
must be disclosed accurately.

这意味着当前依赖树并不只包含 MIT、Apache-2.0 和 BSD 家族许可证。锁文件中还
包含 MPL-2.0 与 BlueOak-1.0.0 依赖，必须如实披露。

<!-- CANVAST_LICENSE_INVENTORY_START -->
## Machine-Verified Inventory / 机器校验清单

> This block is generated by `scripts/license-inventory.mjs` from the checked-in
> lockfile and package manifest. Do not edit it by hand.
> 本区块由 `scripts/license-inventory.mjs` 根据已检入的锁文件和包清单生成，请勿手工修改。

- Inventory SHA-256: `94fd22ea0888c06ec5af9166c0c80706a6f09ef8fb5e95dde0a76922b8131a65`
- Package-lock SHA-256: `6a0860de60528c9034f52600288ba41caaa01b287d1056fe4e6ba2e4d71fd1fe`
- Inventory package count: `349`
- Direct runtime dependency count: `7`

### License Counts / 许可证计数

| License | Packages |
|---|---:|
| `0BSD` | 2 |
| `Apache-2.0` | 94 |
| `BlueOak-1.0.0` | 5 |
| `BSD-3-Clause` | 25 |
| `ISC` | 10 |
| `MIT` | 201 |
| `MPL-2.0` | 12 |

### Direct Runtime Dependencies / 直接运行时依赖

| Package | Requested | Resolved | License |
|---|---|---|---|
| `@earendil-works/pi-ai` | `^0.84.2` | `0.84.2` | `MIT` |
| `@earendil-works/pi-coding-agent` | `^0.84.2` | `0.84.2` | `MIT` |
| `@earendil-works/pi-tui` | `^0.84.2` | `0.84.2` | `MIT` |
| `@sinclair/typebox` | `^0.34.52` | `0.34.52` | `MIT` |
| `chrono-node` | `^2.10.1` | `2.10.1` | `MIT` |
| `pdf-parse` | `^2.4.5` | `2.4.5` | `Apache-2.0` |
| `typebox` | `^1.3.7` | `1.3.7` | `MIT` |
<!-- CANVAST_LICENSE_INVENTORY_END -->

## Direct Runtime Dependencies / 直接运行时依赖

These are the packages declared in `package.json` under `dependencies`.

以下为 `package.json` 中 `dependencies` 声明的直接运行时依赖。

| Package | License | Notes |
|---|---|---|
| `@earendil-works/pi-ai` | `MIT` | Multi-provider model API integration used by the runtime |
| `@earendil-works/pi-coding-agent` | `MIT` | Underlying coding-agent runtime and CLI foundation |
| `@earendil-works/pi-tui` | `MIT` | Terminal UI components |
| `@sinclair/typebox` | `MIT` | Schema definitions for tools and structured inputs |
| `chrono-node` | `MIT` | Date and time parsing |
| `pdf-parse` | `Apache-2.0` | PDF text extraction |
| `typebox` | `MIT` | Additional schema utility package |

## Notable Transitive Dependencies / 重要传递依赖

### MPL-2.0 packages

The current lockfile includes `lightningcss` and its platform packages under
`MPL-2.0`:

当前锁文件包含 `MPL-2.0` 许可的 `lightningcss` 及其平台包：

- `lightningcss`
- `lightningcss-android-arm64`
- `lightningcss-darwin-arm64`
- `lightningcss-darwin-x64`
- `lightningcss-freebsd-x64`
- `lightningcss-linux-arm-gnueabihf`
- `lightningcss-linux-arm64-gnu`
- `lightningcss-linux-arm64-musl`
- `lightningcss-linux-x64-gnu`
- `lightningcss-linux-x64-musl`
- `lightningcss-win32-arm64-msvc`
- `lightningcss-win32-x64-msvc`

These packages enter through the installed dependency tree and must not be
described as MIT/Apache/BSD-only software.

这些包通过已安装依赖树进入当前项目，不能再被描述成“全部都是 MIT/Apache/BSD”。

### BlueOak-1.0.0 packages

The current lockfile also includes transitive packages under `BlueOak-1.0.0`:

当前锁文件还包含 `BlueOak-1.0.0` 的传递依赖：

- `@earendil-works/pi-coding-agent/node_modules/glob`
- `@earendil-works/pi-coding-agent/node_modules/lru-cache`
- `@earendil-works/pi-coding-agent/node_modules/minimatch`
- `@earendil-works/pi-coding-agent/node_modules/minipass`
- `@earendil-works/pi-coding-agent/node_modules/path-scurry`

These are transitive dependencies in the installed npm tree, not historical
reference material.

这些都是当前 npm 安装树中的传递依赖，不是历史参考材料。

### Other notable transitive examples

Representative transitive packages visible in the current lockfile:

当前锁文件中可见的部分代表性传递依赖包括：

| Package | License | Notes |
|---|---|---|
| `pdfjs-dist` | `Apache-2.0` | PDF engine used by `pdf-parse` |
| `@napi-rs/canvas` | `MIT` | Native canvas dependency used in the PDF stack |
| `vite` | `MIT` | Present in the installed toolchain |
| `typescript` | `Apache-2.0` | Type checking and build tooling |
| `vitest` | `MIT` | Test runner |

## Runtime-Referenced External Library / 运行时引用的外部库

The Canvas HTML export documentation states that the generated HTML may load
`d3` from a CDN for graph rendering.

Canvas HTML 导出文档说明，生成的 HTML 可能通过 CDN 加载 `d3` 来渲染图谱。

| Library | License | Usage |
|---|---|---|
| `d3` | `ISC` | Optional client-side graph rendering for exported Canvas HTML |

This is an external runtime reference for generated output, not a package
declared in this repository's `dependencies`.

这是导出产物中的外部运行时引用，不是本仓库 `dependencies` 中直接声明的包。

## Exclusions and Boundaries / 排除项与边界

- `reference-extensions/` is excluded from the public product package policy and
  is not described here as a shipped dependency source.
- Historical research, comparison artifacts, and local evaluation materials are
  not product dependencies merely because they exist in a working repository.
- External service boundaries are documented separately in
  `docs/reference/external-services.md`.

- `reference-extensions/` 已被排除在公开产品包策略之外，因此这里不把它写成随产品
  一起分发的依赖来源。
- 历史研究材料、对比产物和本地评测材料即使存在于工作仓库中，也不等于产品依赖。
- 外部服务边界另见 `docs/reference/external-services.md`。

## Notes / 说明

- This notice file is a repository-level inventory summary, not a replacement
  for the original license texts of each dependency.
- Users distributing Canvast should preserve the repository `LICENSE`,
  `NOTICE`, and this notice file, and should review dependency updates when the
  lockfile changes.
- When dependencies change, regenerate or review this inventory against the
  current `package-lock.json`.

- 本文件是仓库级依赖清单摘要，不替代各依赖自身的原始许可证文本。
- 分发 Canvast 时，应保留仓库中的 `LICENSE`、`NOTICE` 与本声明文件，并在锁文件
  变化后重新审查依赖清单。
- 依赖发生变化时，应基于最新 `package-lock.json` 重新核对本清单。
